openai>=1.14.0
# opencv-python: On Raspberry Pi, install via apt (python3-opencv) instead of pip.
# On desktop, pip install works fine.
opencv-python>=4.9.0
SpeechRecognition>=3.10.0
python-dotenv>=1.0.0
chromadb>=0.4.0
numpy>=1.24.0
pyzbar>=0.1.9
Pillow>=10.0.0
onnxruntime>=1.15.0
requests>=2.28.0
# gpiozero: only needed on Raspberry Pi (optional on desktop)
gpiozero>=2.0; sys_platform == "linux"
